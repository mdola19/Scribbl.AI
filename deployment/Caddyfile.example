# Install Caddy on your VPS, copy this to /etc/caddy/Caddyfile (or merge),
# replace your-domain.com, then: sudo systemctl reload caddy
#
# Assumes docker compose publishes the app on host port 3000 (see docker-compose.yml).

your-domain.com {
  encode gzip zstd
  reverse_proxy localhost:3000
}
