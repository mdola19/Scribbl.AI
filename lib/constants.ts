/** Default live snapshot interval (ms) */
export const DEFAULT_POLL_INTERVAL_MS = 4000;

/** Round length in seconds */
export const ROUND_SECONDS = 120;

/** Max width/height for snapshots sent to Ollama */
export const SNAPSHOT_MAX_EDGE = 512;

/** JPEG quality for compressed snapshots */
export const SNAPSHOT_JPEG_QUALITY = 0.82;
